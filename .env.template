# Application
APP_NAME="Common Portal Platform"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost:8080

# Logging
LOG_CHANNEL=stack
LOG_LEVEL=debug

# Database (Local Docker)
DB_CONNECTION=pgsql
DB_HOST=postgres
DB_PORT=5432
DB_DATABASE=platform
DB_USERNAME=platform
DB_PASSWORD=secret

# Database (Managed - uncomment for production)
# DB_HOST=your-db-cluster.db.ondigitalocean.com
# DB_PORT=25060
# DB_SSLMODE=require

# Redis
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=null

# Cache & Session
CACHE_DRIVER=redis
SESSION_DRIVER=redis
SESSION_LIFETIME=120

# Queue
QUEUE_CONNECTION=redis

# Mail (configure for your provider)
MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# Multi-tenancy
TENANT_DOMAIN=localhost

# Branding defaults
DEFAULT_BRAND_NAME="Common Portal"
DEFAULT_BRAND_COLOR="#3B82F6"

# WalletIDs.net (Wallet Factory + Monitoring)
WALLETIDS_BASE_URL=https://walletids.net/api
WALLETIDS_API_KEY=
WALLETIDS_WEBHOOK_SECRET=

# Solana RPC
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# SH Financial (Direct Debit)
SHF_API_URL=https://api.sh-payments.com
SHF_CLIENT_ID=your-client-id
SHF_CLIENT_SECRET=your-client-secret
SHF_SCOPE=apiv1.programme
SHF_CREDITOR_ID=LT10ZZZ188607684
