# Administrator Controllers

Platform-wide admin functions. Access restricted to members with `is_platform_administrator = true`. Manages all accounts, members, and system-level settings across the entire platform.
